The transformer.py file can take input from args as well as the console.

You can provide the argv arguments as:
"messageFile.txt instructionFile.txt outputFile.txt e"

Or you can individually specify the name of the file in the console.

Please put in the extension ".txt" while naming the file.